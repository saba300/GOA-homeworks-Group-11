//1) გამოიყენეთ forEach მეთოდი და გააკეთეთ 
// 2 მაგალითი, ამის შემდეგ თქვენი ხელით შექმენით forEach მეთოდის კლონი

const numbers = [1, 2, 3, 4, 5];

numbers.forEach(function(num) {
    console.log(num);  // Logs each number to the console
});
